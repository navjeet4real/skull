import React, { Fragment } from "react";
import Header from "./Header";
import MemeGenerator from "./MemeGenerator";

const Home = () => {
  return (
    <Fragment>
      <MemeGenerator />
    </Fragment>
  );
};

export default Home;
